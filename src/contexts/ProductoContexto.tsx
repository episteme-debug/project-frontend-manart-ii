/* import { ProductoRespuesta } from "@/interfaces/ProductoInterfaz";
import React, { useContext} from "react"

type TipoContextoProducto = {
    producto: ProductoRespuesta | null
    setProucto: React.Dispatch<React.SetStateAction<ProductoRespuesta | null>>
}

const ContextoProducto = React.createContext<TipoContextoProducto | null>(null)

export function ProveedorProducto({ children } : { children: React.ReactNode}) {
    const [producto, setProducto]
} */